# Direct dependencies for The Agentic AI Handbook code examples.
#
# The supported way to set up the environment is uv, from the repository root:
#
#     uv sync --all-groups
#
# That installs these packages AND their transitive dependencies at the exact
# versions recorded in uv.lock, which is committed alongside this file.
# This file is a convenience mirror for readers who cannot use uv; the
# versions below are pinned to match uv.lock.
#
# Requires Python 3.13 (crewai does not yet support 3.14).

# Model provider SDKs
openai==2.54.0
anthropic==1.3.0

# LangChain ecosystem
langchain==1.4.0
langchain-core==1.6.1
langchain-openai==1.6.0
langchain-anthropic==1.7.1
langchain-mcp-adapters==0.3.2
langgraph==1.2.11
langsmith==0.12.1

# Agent frameworks
crewai==1.15.18

# Model Context Protocol (MCP)
mcp==1.28.1

# Agent2Agent (A2A) Protocol
a2a-sdk==1.1.2

# Memory, embeddings and tokenization
chromadb==1.1.1
tiktoken==0.14.0

# Databases
asyncpg==0.31.0

# HTTP and web servers
requests==2.34.2
flask==3.1.3
sseclient-py==1.9.0

# Data science and ML
numpy==2.5.2
scikit-learn==1.9.0

# Security and auth
pyjwt==2.13.0

# Monitoring
prometheus-client==0.26.0

# Utilities
python-dotenv==1.2.3
jsonschema==4.26.0

# Testing (the chapter 3 harness runs pytest as its verifier)
pytest==9.1.1

# Jupyter tooling (the 'dev' dependency group in pyproject.toml)
ipykernel==7.3.0
jupyter==1.1.1
jupyterlab==4.6.3
notebook==7.6.2
